export const Config = {
  // Set to false when real V2 backend is available
  USE_MOCK_API: true,
  API_BASE_URL: 'http://10.0.2.2:8000', // Android emulator → localhost
  API_TIMEOUT: 30000,
  AUDIO_CHUNK_DURATION_MS: 3000,
  ENROLL_DURATION_SECONDS: 5,
};
