# Optional: load API keys from backend/.env
python-dotenv>=1.0.0

# PDF report (SOAP JSON → HTML → PDF)
jinja2>=3.1.0
weasyprint>=62.0

# API server
fastapi>=0.110.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# Database (SQLite via SQLAlchemy — no extra driver needed)
sqlalchemy>=2.0.0

# Password hashing
bcrypt==4.0.1
passlib>=1.7.0

# JWT auth
python-jose[cryptography]>=3.5.0

# ML / Audio processing
torch
faster-whisper
speechbrain
silero-vad
pydub
